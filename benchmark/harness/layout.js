/**
 * Identical viewport and row geometry for all three libraries, so each one shows
 * the same number of rows at once and scrolls the same distance per node.
 * 62px is the default row height of both react-sortable-tree lineages.
 */
export const ROW_HEIGHT = 62
export const VIEWPORT = { height: 600, width: 900, overflow: 'auto' }
